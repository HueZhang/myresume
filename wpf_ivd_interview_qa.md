# WPF 桌面开发面试准备手册（IVD 医疗器械公司 · 张昊宇）

> 场景：无 JD，目标只有一个——让面试官确信你能**直接上手 WPF 开发**，并且有**医疗仪器对接 + HIS 集成**的实战能力。
>
> 背诵优先级：
> - ★★★（必背）：自我介绍、WPF vs WinForms 对比、UI 线程/后台线程调度、数据绑定与 MVVM、设备通信基础
> - ★★（熟练）：HIS/LIS 集成、场景设计题、项目深挖口径
> - ★（了解）：STAR 故事、行为面、反问

---

## 1. 岗位分析与考点预测（无 JD 推断）

### 1.1 IVD 医疗器械公司的桌面端岗位常见职责

IVD（体外诊断）公司的软件通常分两类：**仪器配套软件**（控制/采集检验仪器数据）和**数据管理/报告软件**（样本、结果、质控、报告打印、与 LIS/HIS 交互）。结合你的经历，最可能的工作内容：

1. WPF 客户端开发与维护（采样界面、结果展示、质控图、报告单）
2. 检验仪器数据采集：串口（RS232/RS485）、TCP/IP，私有协议或 ASTM/LIS2-A2
3. 与 LIS/HIS 对接：患者/医嘱/结果的双向同步（HL7、API、中间表、MQ）
4. 现场实施与联调：去检验科/实验室对接真实仪器
5. 离线/断网场景：数据本地缓存、断点续传、补报

### 1.2 面试官会测什么（按概率排序）

| 概率 | 考点 | 你的证据 |
|------|------|----------|
| ★★★ | WPF 上手能力（XAML、绑定、MVVM、依赖属性、样式模板） | 血透工作站 WPF+MVVM、HIS 客户端 WPF+WinForm 混合、WPF 工具 |
| ★★★ | WinForms→WPF 差异与优势 | 你两边都写过，天然有对比发言权 |
| ★★★ | UI 线程/后台线程调度、界面不卡 | 设备采集、批量上报、异步处理经历 |
| ★★★ | 设备对接（串口/TCP/协议解析/断线重连） | 血压计、体重秤、透析机、检验设备、读卡器 |
| ★★★ | 项目深挖（做了什么、为什么这么设计、代价） | 血透/HDIS、HIS、IOT 采集 |
| ★★ | 医疗系统集成（HIS/LIS/PACS/EMR/CNRDS） | 多系统对接、数据上报 |
| ★★ | 场景/设计题（检验仪器客户端怎么设计） | 上述经历组合回答 |
| ★ | 行为面、自我介绍、反问 | 见后文 |

### 1.3 你的三个卖点（所有回答都要往这上面引）

1. **直接上手 WPF**：不是转行，是 5 年 WPF/WinForms 双线实战，MVVM、CommunityToolkit、自定义控件、XAML 模板都是生产级。
2. **医疗行业即插即用**：HIS/HDIS/血透/LIS/PACS/EMR/CNRDS 全部真实对接过，业务流程和行业黑话（医嘱、申请单、结果回传、质控）都懂。
3. **硬件集成是稀缺能力**：串口/RS485/TCP、多厂商设备驱动抽象、断线重连、协议解析，这正是 IVD 公司最头疼、也最需要的能力。

---

## 2. 两分钟自我介绍（口播稿）

> 中文语速约 230~260 字/分钟，下面这版约 500 字，念出来 2 分钟左右。建议录下来练 3 遍，掐表。

面试官您好，我叫张昊宇，5 年 .NET 开发经验，最近一份工作在医疗信息化公司做 C# 开发工程师，求职方向是 WPF 桌面开发。

这几年的经验可以分成三块：第一块是桌面端。我在华墨信息做过血液透析工作站的 WPF 客户端和 HIS 客户端，全程使用 MVVM 架构，XAML 样式模板、数据绑定、自定义控件、CommunityToolkit.Mvvm 这些都有完整的生产级实践；HIS 客户端是 WPF 和 WinForms 混合架构，所以两边我都能直接上手。

第二块是设备和硬件对接。我用串口、RS485 和 Socket TCP/IP 对接过血压计、体重秤、透析机、检验设备和读卡器，做过设备驱动抽象层——不同厂商的设备只要实现统一接口就能接入；也实际处理过协议解析、数据校验、断线重连、异常恢复这些问题，保证设备数据稳定、界面不卡。

第三块是后端和系统集成。我用 .NET 8 写过 Web API，用过 EF Core、Dapper、Redis、RabbitMQ；主导过 HIS 与 LIS、PACS、EMR 的接口对接，也做过全国血液净化病例登记系统 CNRDS 的数据上报，负责数据抽取、校验、补报和状态跟踪。

我最想强调的是，这些不是演示项目，而是真实医疗环境里的交付：设备数据要从仪器上稳定采上来，数据不能丢，断网要能补报，界面不能卡——这些场景我都实际解决过。所以对 IVD 公司来说，我是那种入职后能直接接手 WPF 客户端开发、同时能对接检验仪器和 HIS/LIS 的候选人。希望有机会加入贵公司，把这块业务做深。

**30 秒精简版：**

我叫张昊宇，5 年 .NET 开发，主攻 WPF 桌面端。在医疗信息化公司做过血透工作站和 HIS 客户端的 WPF/MVVM 开发，用串口、TCP 对接过血压计、体重秤、透析机等设备，主导过 HIS 与 LIS/PACS/EMR 的对接和 CNRDS 数据上报。我能直接上手 WPF，也懂医疗仪器对接——这正是 IVD 桌面端岗位需要的组合。

---

## 3. WPF 核心问答（★必背）

### Q1：介绍一下 WPF，它和 WinForms 的本质区别是什么？

**答案要点：** WPF 是微软基于 .NET 的桌面 UI 框架，核心是 XAML 声明式 UI + 数据驱动。和 WinForms 相比，本质区别是：

- **渲染方式**：WinForms 基于 GDI+ 和 Win32 句柄控件；WPF 基于 DirectX 保留模式，矢量渲染、硬件加速、高 DPI 自适应。
- **编程模型**：WinForms 是事件驱动 + 控件拖拽 + code-behind；WPF 是数据驱动，UI 用 XAML 描述，逻辑通过数据绑定和命令与 UI 解耦。
- **可定制性**：WinForms 控件外观靠自绘，很费劲；WPF 任何控件都能通过 ControlTemplate 完全重定义外观。
- **线程模型**：两者都是单 UI 线程 + 消息循环，但 WPF 用 Dispatcher，配合 async/await 更自然。

### Q2：WPF 相比 WinForms 的优势是什么？（高频）

**答案要点：** 面试官想听的是"你能说出 WPF 强在哪"，建议按数据、UI、维护性三层讲：

1. **UI 表现力**：样式/模板/触发器体系 + 动画 + 矢量渲染，能做出现代化界面（医疗软件现在也要求好看、好用）。
2. **数据驱动**：绑定 + MVVM，UI 和逻辑彻底分离，ViewModel 可单元测试，复杂业务（医嘱、结果、多窗口联动）可维护性远高于事件堆 code-behind。
3. **可定制/可复用**：资源字典管理主题，一套 Style 全局生效，换肤只需换资源；自定义控件比 WinForms 自绘容易得多。
4. **渲染与性能**：硬件加速、虚拟化（VirtualizingStackPanel）、绑定批量更新，大数据量列表比 WinForms 的 DataGridView 方案更有优势。

**追问：WPF 有什么劣势？** 学习曲线高、启动速度和内存占用相对高、渲染复杂 UI 不当会卡；所以需要 MVVM 规范 + 性能优化手段（虚拟化、冻结 Freezable、减少绑定）。

### Q3：数据绑定的原理？（必背）

**答案要点：**

- 绑定四要素：源（Source）、路径（Path）、目标（Target，必须是依赖属性）、模式（Mode：OneWay/TwoWay/OneTime/OneWayToSource）。
- 源对象要实现 `INotifyPropertyChanged`，属性变化时发通知，WPF 绑定引擎自动更新 UI；UI 变化回写源靠 `UpdateSourceTrigger`（默认 TextBox 是 LostFocus，可改 PropertyChanged）。
- 数据转换用 `IValueConverter`（如 bool→Visibility）；多值用 MultiBinding。
- ViewModel 用 CommunityToolkit.Mvvm 的 `[ObservableProperty]` 源生成器，自动实现通知，不用手写样板代码。

```csharp
public partial class MainViewModel : ObservableObject
{
    [ObservableProperty]
    private string patientName;
}
```

### Q4：依赖属性是什么？和普通属性有什么区别？值优先级？

**答案要点：**

- 依赖属性（DependencyProperty）是 WPF 属性系统的核心，支持绑定、样式、动画、值继承、默认值元数据。
- 与 CLR 属性区别：普通属性就是个字段包装；依赖属性由 WPF 属性系统统一管理，值可以来自本地值、样式、模板、动画、继承、默认值。
- **值优先级（从高到低）**：动画 > 本地值 > 模板触发器 > 样式触发器 > 模板设置器 > 主题样式 > 继承值 > 默认值。记住"动画最高、本地值第二"，排查样式覆盖问题时按这个顺序想。

```csharp
public static readonly DependencyProperty IsBusyProperty =
    DependencyProperty.Register(nameof(IsBusy), typeof(bool), typeof(MyControl),
        new PropertyMetadata(false));

public bool IsBusy
{
    get => (bool)GetValue(IsBusyProperty);
    set => SetValue(IsBusyProperty, value);
}
```

### Q5：路由事件和传统事件的区别？命令解决了什么问题？

**答案要点：**

- 路由事件沿可视树传播：冒泡（Bubble，向上）、隧道（Tunnel，Preview 前缀，向下）。好处是父容器可以统一处理子元素事件（比如整个窗口捕获条码输入），不需要每个控件挂事件。
- 命令（ICommand）把"按钮点击"和"业务逻辑"解耦：ViewModel 里定义命令，XAML 绑定，`CanExecute` 控制可用性；比 Click 事件可测试、可复用（多个按钮绑定同一命令）。CommunityToolkit 的 `RelayCommand` / `AsyncRelayCommand` 直接支持异步命令。

### Q6：Style、ControlTemplate、DataTemplate、Trigger 的区别？（高频）

**答案要点：**

- **Style**：定义控件的一组属性（Setter），按 TargetType 应用，可 BasedOn 继承。
- **ControlTemplate**：重定义控件的**外观结构**（Button 的边框、背景、内容容器），是"控件长什么样"。
- **DataTemplate**：定义**数据的呈现方式**（一个 Patient 对象在列表里显示成什么），是"数据长什么样"。
- **Trigger**：根据条件改变样式，包括 PropertyTrigger（属性变化）、DataTrigger（数据条件）、EventTrigger（配合动画）。

一句话区分：Style 设置属性，ControlTemplate 换壳，DataTemplate 换数据展示，Trigger 做状态切换。

### Q7：为什么用 MVVM？ViewModel 之间怎么通信？

**答案要点：**

- 好处：UI 与逻辑分离、可单元测试、团队可并行（前端写 XAML、开发写 ViewModel）、换 UI 不换逻辑。
- ViewModel 职责：持有 UI 状态（ObservableProperty）+ 命令，调用服务层；业务逻辑下沉到 Service，不堆在 ViewModel。
- 通信三种方式：**Messenger**（WeakReferenceMessenger 发送/接收消息，解耦，如"处方发药完成→刷新库存"）、**依赖注入**（构造函数注入其他 VM/服务）、**共享服务**（单例）。

```csharp
WeakReferenceMessenger.Default.Send(new RefreshInventoryMessage());

WeakReferenceMessenger.Default.Register<RefreshInventoryMessage>(this, (r, m) => LoadInventory());
```

### Q8：绑定不生效怎么排查？（高频实战题）

**答案要点（按顺序）：**

1. 确认 `DataContext` 有没有设置（最常犯）；
2. 确认绑定 Path 拼写和大小写（Path 区分大小写）；
3. 确认源对象实现了 `INotifyPropertyChanged`，属性是 public；
4. 看 VS 输出窗口的 Binding 错误（`System.Windows.Data Error: 40`）；
5. 检查 Mode 和 UpdateSourceTrigger；
6. 检查是不是有转换器/样式把值覆盖了。

### Q9：UserControl 和 CustomControl 怎么选？自定义控件怎么做？

**答案要点：**

- **UserControl**：组合现有控件，适合页面/模块级复用（如"患者信息卡"），开发快；但难以换肤。
- **CustomControl**：继承 Control，用 ControlTemplate + 依赖属性重定义，适合"新控件"且要支持主题/样式定制（如医疗图表控件、状态指示灯）。
- 做法：继承 Control → 定义依赖属性 → 提供 `Themes/Generic.xaml` 默认样式（`DefaultStyleKey`）→ 用 TemplateBinding 绑定。
- 顺带提一个生产经验：模板里的元素用 TemplateBinding/绑定而不是写死，才能做到换主题。

### Q10：资源字典和主题怎么组织？

**答案要点：** App.xaml 合并资源字典（MergedDictionaries），按模块拆分（Colors、Styles、Templates、Converters），避免一个超大字典。换肤用 DynamicResource 引用 + 运行时替换资源字典。StaticResource 编译期解析，DynamicResource 运行时解析（可换主题，但有性能开销）。

### Q11：WPF 性能优化有哪些手段？（高频）

**答案要点：**

- 列表用 `VirtualizingStackPanel` 虚拟化，`IsVirtualizing="True"`；避免整个列表全部实例化。
- 减少绑定数量，特别是高频更新的属性；批量更新数据（集合变化一次通知，而不是逐条 Add）。
- Freezable 对象（Brush、Transform）尽量冻结（`Freeze()`），减少线程通知开销。
- 避免滥用透明度动画、Effect（DropShadowEffect 是软件渲染，很贵）。
- 大数据量先后台处理，再一次性在 UI 线程更新；分页/增量加载。
- 启动优化：延迟加载不必要模块、资源按需合并、减少启动时初始化。

### Q12：WPF 客户端怎么部署和更新？

**答案要点：** 常用 ClickOnce（简单、自动更新）、Squirrel/NSIS（安装包 + 增量更新）、MSIX。医疗内网环境常考虑：离线安装包 + 手动更新 + 版本检测接口（客户端启动时请求最新版本，比较后下载覆盖）。可以补一句"我之前做的客户端更新逻辑是先校验版本→下载→校验文件→备份→替换→回滚"。

### Q13：WPF 和 WinForms 能混用吗？

**答案要点：** 可以。WPF 里用 `WindowsFormsHost` 承载 WinForms 控件（如老表格控件），WinForms 里用 `ElementHost` 承载 WPF 内容。我们 HIS 客户端就是 WPF + WinForm 混合架构——老模块不动、新模块用 WPF，逐步迁移。注意两者渲染层级不同，混合时注意焦点和性能。

---

## 4. WinForms → WPF 迁移与全面对比（★必背）

> 你两边都写过，这是你最能出彩的部分。面试官问"WinForms 转 WPF"时，核心考点是你**真的理解差异**，而不是背概念。

### Q1：从 WinForms 转 WPF，最大的观念转变是什么？

**答案要点：** 从**事件驱动**变成**数据驱动**。WinForms 是"控件拉个事件、在 code-behind 里写逻辑"；WPF 是"XAML 声明 UI + 绑定数据 + 命令触发逻辑"。写 WPF 时第一反应应该是"这个 UI 绑什么数据、状态变化怎么通知"，而不是"哪个控件触发哪个方法"。其次是控件模型：WinForms 控件是句柄，WPF 控件无句柄、可模板化。

### Q2：WPF 和 WinForms 的多维度对比（背诵这张表）

| 维度 | WinForms | WPF |
|------|----------|-----|
| 渲染 | GDI+ 立即模式，基于 Win32 句柄控件 | DirectX 保留模式，矢量渲染、硬件加速、高 DPI 自适应 |
| UI 构建 | 设计器拖控件 + 事件 + code-behind | XAML 声明式，数据驱动 |
| 控件模型 | 有句柄，样式定制靠 OwnerDraw 自绘 | 无句柄，ControlTemplate 完全换外观 |
| 数据绑定 | 弱，BindingSource + 控件属性 | 强：依赖属性 + INotifyPropertyChanged + 转换器 + 多绑定 |
| 样式/主题 | 基本靠自绘和第三方皮肤 | Style / Template / Trigger / 资源字典，全局主题 |
| 布局 | 绝对坐标 + Anchor/Dock | 布局容器（Grid/StackPanel/DockPanel），可自适应缩放 |
| 动画 | 基本不支持 | 声明式动画（Storyboard、DoubleAnimation） |
| 线程模型 | 单 UI 线程 + 消息循环，跨线程必须 Invoke/BeginInvoke | 同样单 UI 线程 + Dispatcher，但配合 async/await 更自然 |
| 学习曲线 | 低，上手快 | 高，但上限高 |
| 性能 | 简单界面轻快；大数据量列表弱 | 复杂视觉树/绑定不当会卡；有虚拟化等优化手段 |
| 生态 | DevExpress/Telerik 成熟 | 控件生态丰富（DevExpress、HandyControl 等） |
| 适合场景 | 工具类、快速交付、老系统维护 | 现代化界面、复杂交互、医疗工作站这类重业务客户端 |

### Q3：两者的 UI 线程模型一样吗？差别在哪？

**答案要点：** 本质一样：都是**单 UI 线程 + 消息循环**，UI 控件只能由创建它的线程访问。差别在 API 和配合度：

- WinForms 用 `Invoke`（同步等待）/ `BeginInvoke`（异步投递）；WPF 用 `Dispatcher.Invoke` / `Dispatcher.BeginInvoke`，且 Dispatcher 有优先级（DataBind、Render 等）。
- WPF 的绑定引擎会自动把属性通知调度到 UI 线程，配合 `async/await` 写异步更新非常自然，不像 WinForms 时代手动 Invoke 一堆样板代码。

### Q4：把现有 WinForms 项目迁到 WPF，你会怎么做？

**答案要点（迁移策略）：**

1. **先抽业务层**：把 code-behind 里的业务逻辑下沉到独立的 Service/Manager 类（不依赖 UI），这是复用和测试的前提。
2. **逐模块替换**：新功能用 WPF 写，老模块用 WindowsFormsHost 先承载过渡，成熟一个替换一个，不要大爆炸重写。
3. **统一 MVVM**：先搭 MVVM 基建（ViewModel 基类、命令、导航、DI），再按模块迁。
4. **数据层不动**：数据库访问、接口调用层直接复用，先换壳不换芯。
5. **注意差异点**：布局从 Anchor/Dock 改成布局容器；事件改命令；跨线程更新从 Invoke 改 Dispatcher/async；老第三方 WinForms 控件用 WindowsFormsHost 过渡。

**追问：迁移中最大的坑？** 两个：一是把 WinForms 的 code-behind 思维带进 WPF（事件堆逻辑，MVVM 名存实亡）；二是性能（老 WinForms 列表直接搬成 ItemsControl 不虚拟化会卡，必须开虚拟化）。

---

## 5. UI 线程与后台线程调度（★必背，你的重点）

> 这一节是面试官验证"你能不能写出不卡的 WPF 客户端"的关键。所有设备采集、上报、查询卡不卡都归到这里。

### Q1：为什么 UI 控件不能跨线程访问？

**答案要点：**

- WPF 控件是线程关联的（DispatcherObject），UI 线程创建 UI，控件状态只由 UI 线程维护。
- 多线程同时操作 UI 需要锁，会引入复杂度、死锁风险和难以排查的竞态；WPF 用"单线程 + 消息队列"模型，牺牲一点并发换稳定性和可预测性。
- 跨线程直接改 UI 会抛异常："调用线程无法访问此对象，因为另一个线程拥有该对象"。

### Q2：WinForms 和 WPF 的跨线程更新 API 对照？

**答案要点：**

| 场景 | WinForms | WPF |
|------|----------|-----|
| 回到 UI 线程（同步） | `control.Invoke(...)` | `dispatcher.Invoke(...)` |
| 回到 UI 线程（异步） | `control.BeginInvoke(...)` | `dispatcher.BeginInvoke(...)` |
| 检查是否在 UI 线程 | `control.InvokeRequired` | `dispatcher.CheckAccess()` |
| 现代写法 | `await Task.Run(...)` 后直接更新（同步上下文自动回到 UI 线程） | 同样，`await` 自动回 UI 线程 |

### Q3：Dispatcher.Invoke 和 BeginInvoke 的区别？DispatcherPriority 是什么？

**答案要点：**

- `Invoke` 同步：当前线程阻塞等 UI 线程执行完；`BeginInvoke` 异步：投递到 Dispatcher 队列立即返回。
- **UI 线程里调用 Invoke 等自己**会死锁（经典错误）；后台线程用 Invoke 等 UI，UI 又反过来等后台任务，也会死锁。
- `DispatcherPriority` 控制队列执行顺序：如 Background(4) < Input(5) < Render(7) < DataBind(8) < Normal(9)。大量数据刷新时可以用 DataBind/Render 优先级，让绘制和绑定批量处理，避免界面"一帧一卡"。

### Q4：async/await 为什么能自动回到 UI 线程？（必背原理）

**答案要点：**

- `await` 会捕获当前线程的 `SynchronizationContext`（WPF UI 线程是 `DispatcherSynchronizationContext`），continuation（await 后面的代码）被投递回这个上下文执行，所以**默认自动回到 UI 线程**。
- 这意味着耗时操作后可以直接更新控件：`var data = await _service.LoadAsync(); Items = data;` 不需要手动 Dispatcher。
- 注意：如果在后台线程 await（没有 UI 同步上下文），continuation 就在线程池执行；用 `ConfigureAwait(false)` 会跳过捕获，不回到 UI 线程。

### Q5：async/await 和 Task.Run 怎么选？

**答案要点：**

- **I/O 密集**（数据库查询、HTTP 调用、串口读写、文件）：用 `async/await` 调用真异步 API（`ReadAsync`、`SendAsync`），不占线程，UI 不卡。
- **CPU 密集**（解析大量数据、转码、计算）：用 `Task.Run` 扔到线程池，避免阻塞 UI 线程。
- 判断一句话：**没有真异步 API 的耗时活才需要 Task.Run；有就全程 async**。设备协议解析属于 CPU 密集，一般放后台线程/线程池。

### Q6：后台线程更新 INotifyPropertyChanged 一般不崩，ObservableCollection 却崩，为什么？（高频深挖）

**答案要点：**

- `PropertyChanged` 通知只携带属性名，WPF 绑定引擎收到通知后会**调度到 UI 线程重新读取属性值**，所以后台线程发属性通知一般不会抛异常（但不推荐，存在竞态，最佳实践仍是 UI 线程更新）。
- `ObservableCollection` 的 `CollectionChanged` 携带**实际变更项**，WPF 出于性能考虑**不会自动封送集合变更**——后台线程 Add/Remove 会导致 UI 枚举集合时崩溃或数据不一致。
- 解决：`BindingOperations.EnableCollectionSynchronization(collection, lockObj)` + 修改时加锁；或者后台线程只准备数据，一次性在 UI 线程重建/批量更新集合。

```csharp
private readonly object _lock = new();
private ObservableCollection<Item> _items = new();

public MainViewModel()
{
    BindingOperations.EnableCollectionSynchronization(_items, _lock);
}

public void AddFromBackground(Item item)
{
    lock (_lock) _items.Add(item); // 后台线程安全修改
}
```

### Q7：设备采集场景的线程模型怎么设计？（你的亮点题）

**答案要点：** 生产-消费模式：

1. **采集线程**（SerialPort.DataReceived 触发的后台线程/TCP 接收循环）只负责读字节流和协议解析，不碰 UI。
2. **解析结果放进线程安全队列/通道**（`Channel<T>` 或锁保护的 Queue），或者通过 Dispatcher 节流投递。
3. **UI 更新节流**：设备高频上报（如体重秤连续称重、仪器状态心跳）时，不要每包都刷新 UI，用时间戳节流（100ms 合并）或只取最新值。
4. **取消与退出**：用 `CancellationTokenSource`，窗口关闭时取消采集循环、释放串口/TCP 资源，避免线程泄漏。

```csharp
private DateTime _lastUiUpdate = DateTime.MinValue;

private void OnDeviceData(byte[] data)
{
    var parsed = ParseProtocol(data);            // 后台线程
    if (DateTime.Now - _lastUiUpdate < TimeSpan.FromMilliseconds(100))
        return;                                  // 节流
    _lastUiUpdate = DateTime.Now;
    Dispatcher.BeginInvoke(() => CurrentWeight = parsed.Weight); // 只投递最新值
}
```

### Q8：界面卡顿怎么排查？（高频实战题）

**答案要点：**

1. 先确认是 UI 线程卡还是渲染卡：看主线程是否长时间忙碌（Visual Studio 的"并行堆栈"/PerfView，或卡顿时 CPU 是否飙高）。
2. 常见原因排查顺序：
   - **同步阻塞**：UI 线程上有同步 I/O（数据库查询、HTTP、串口 Read 阻塞等待）；
   - **Invoke/Dispatcher 风暴**：高频 Invoke 导致 UI 线程忙于执行回调；
   - **可视化树过大**：列表不虚拟化，一次性渲染几千行；
   - **绑定过多/转换器过重**：高频属性每次变化触发大量绑定；
   - **布局/重绘频繁**：频繁改大小、动画、Effect。
3. 工具：WPF 的 `PresentationTraceSources`、内存分析看是否事件未解绑导致泄漏。

### Q9：频繁 Invoke 怎么优化？

**答案要点：**

1. 节流：固定时间窗口内只更新一次（上面的示例）；
2. 合并：多条数据攒一批，一次 UI 更新；
3. 只保留最新：设备数据 UI 展示最新一帧即可，中间数据丢弃；
4. 必要时降低刷新频率（如 4Hz 足够人眼，不必 60Hz）。

### Q10：有没有遇到死锁？怎么避免？

**答案要点：** 经典 WPF 死锁：UI 线程 `task.Result`/`.Wait()` 阻塞等待后台任务，而后台任务内部 `await` 又试图回 UI 线程 → 互相等待。避免：

- 全程 `async/await`，永远不用 `.Result`/`.Wait()` 同步阻塞；
- 后台库代码用 `ConfigureAwait(false)`，避免把"回 UI 线程"的期望传给后台任务；
- 设备/IO 操作设计超时（`Task.WhenAny` 或 `CancellationToken`），防止永久等待。

### Q11：BackgroundWorker 和 Task 有什么区别？

**答案要点：** BackgroundWorker 是老 API（事件：DoWork/ProgressChanged/RunWorkerCompleted），适合简单"后台干活 + 进度回 UI"；Task/async/await 是现代方案：更轻、可组合（WhenAll/WhenAny）、支持取消令牌、错误传播自然、配合 UI 同步上下文更简洁。新代码一律 Task，BackgroundWorker 只在老代码维护时遇到。

---

## 6. 设备与硬件对接（IVD 特色，★必背）

> IVD 公司面试官最关心两件事：**你能不能把仪器数据稳定采上来**、**采上来之后界面不卡、数据不丢**。你有真实经验，把"为什么"讲清楚就是加分项。

### Q1：串口通信的基本流程？

**答案要点：**

```csharp
using System.IO.Ports;

var port = new SerialPort("COM3", 9600, Parity.None, 8, StopBits.One);
port.DataReceived += OnDataReceived;   // 后台线程触发
port.Open();
```

要点：

1. 配置：端口号、波特率、数据位、校验位、停止位——要和设备厂商协议文档一致；
2. `DataReceived` 事件在**独立线程**触发，不能直接碰 UI；
3. 用 `BytesToRead` 读完整缓冲，按协议拆包（帧头/帧尾/长度/校验）；
4. 串口是独占资源：异常处理 + 关闭时 Dispose；重连要处理"串口被占用"（设备未插/被其他程序占用）。

### Q2：RS232 和 RS485 的区别？

**答案要点：** RS232 点对点、全双工、抗干扰弱、距离短（15 米内）；RS485 总线式多节点（几十台）、半双工、差分信号抗干扰强、距离长（1200 米）。工业/医疗现场多设备采集通常用 RS485，电脑侧需要 USB/PCI 转 485 转换器。协议层常用 Modbus RTU（NModbus 库）：功能码 03 读保持寄存器、04 读输入寄存器、06 写单寄存器、16 写多寄存器。

### Q3：协议解析怎么处理粘包、半包、校验？

**答案要点：**

- **粘包/半包**：串口/TCP 是字节流，一次 DataReceived 可能包含多条报文或半条报文。方案：维护接收缓冲，按"帧头 + 长度字段"循环拆帧，拆出完整一帧才解析，剩余字节留到下次。
- **校验**：常见奇偶校验、LRC（Modbus ASCII）、CRC16（Modbus RTU）、和校验；校验不过丢弃或请求重发，并记录日志。
- **超时**：发指令后等待应答要有超时（如 3 秒），超时重发 N 次，超过阈值判定设备异常。

### Q4：设备驱动抽象层怎么设计？（你的亮点题）

**答案要点：** 核心是"接口隔离变化"：

```csharp
public interface IDeviceDriver
{
    string DeviceType { get; }
    bool Open();
    bool Close();
    bool Send(byte[] command);
    event EventHandler<DeviceDataEventArgs> DataReceived;
    DeviceStatus Status { get; }
}
```

1. 定义统一接口（Open/Close/Send/DataReceived/Status），血压计、体重秤、透析机各实现一个驱动；
2. 驱动工厂按配置（厂商型号、端口、协议类型）创建实例，配置放 JSON，新增设备只加驱动类和配置，**不改核心采集逻辑**；
3. 上层（采集服务）只依赖接口，不感知具体厂商；
4. 结果：多厂商兼容、可插拔、现场换设备型号不用改代码。

**追问：为什么不用 if/else 直接写在采集逻辑里？** 每加一个厂商就改核心逻辑，风险高、不可测试、现场排查困难；抽象层让新增设备变成"加一个类 + 一份配置"。

### Q5：断线重连策略？

**答案要点：**

1. **检测**：串口用 DataReceived 超时/串口状态事件，TCP 用心跳（应用层定时发 ping，N 秒无响应判离线）；
2. **重连**：指数退避（1s→2s→4s→...封顶 30s），避免设备恢复时反复重连打爆日志；
3. **状态机**：Disconnected → Connecting → Connected → Error，UI 通过状态属性显示"设备离线/重连中"；
4. **数据不丢**：重连期间的数据缓存到内存队列（限制容量），恢复后补发；数据库操作失败走重试表。

### Q6：条码枪的全局键盘钩子怎么做的？（亮点题）

**答案要点：** 条码枪通常是"键盘楔入"方式，扫码等于快速键盘输入。用低级键盘钩子（WH_KEYBOARD_LL）捕获输入流，根据扫码枪的特征（高速输入、前后缀字符）识别一次完整扫码，然后把条码**自动路由到当前焦点单据行**，实现无侵入集成——不用改每个输入框的代码。

注意点：钩子回调必须快（不能做 UI 操作）；要防抖/去重（避免连续扫同一码重复触发）；兼容输入法（中文 IME 开启时按键事件处理要小心）；窗口失去焦点时挂起钩子，避免误收其他程序的输入。

### Q7：TCP 长连接在线监测怎么设计？

**答案要点：** 独立接收循环（`ReadAsync`）+ 心跳机制：客户端定时发心跳、服务端超时无响应判定离线；离线自动告警（UI 状态 + 声音/弹窗）+ 断线自动重连；接收循环处理半包缓存。参考：

```csharp
private async Task ReceiveLoopAsync(CancellationToken ct)
{
    var buffer = new byte[1024];
    while (!ct.IsCancellationRequested)
    {
        var n = await _stream.ReadAsync(buffer.AsMemory(0, buffer.Length), ct);
        if (n > 0) _packetBuffer.Append(buffer.AsSpan(0, n));
        TryParseFrames(); // 拆帧 → 校验 → 触发事件
    }
}
```

### Q8：场景题：让你对接一台检验仪器（如生化分析仪），你会怎么设计？

**答案要点（按顺序讲）：**

1. **先拿协议文档**：接口方式（串口/TCP）、波特率、数据帧格式、命令集、ASTM/LIS2-A2 还是私有协议；
2. **通信层**：串口/TCP 封装 + 接收缓冲 + 拆帧 + 校验 + 超时重发；
3. **驱动层**：实现统一设备接口（复用我的驱动抽象层思路），仪器专属逻辑隔离；
4. **解析层**：ASTM 是标准消息结构（Header/Patient/Order/Result 段），按段解析成对象模型；私有协议按文档做帧映射；
5. **数据处理**：校验（项目编码映射、单位、参考范围、异常标记）、落库、回传 LIS/HIS；
6. **UI**：设备状态、实时结果列表（节流刷新）、历史记录、故障提示；
7. **异常**：断线重连、重发、日志、告警；离线缓存 + 恢复补传。

---

## 7. 医疗系统集成（HIS/LIS/PACS/EMR/CNRDS）

### Q1：WPF 客户端和后端 API 怎么配合？

**答案要点：** 客户端只做 UI 和交互，通过 HttpClient/Refit 调 Web API；登录拿 Token 存内存（或 SecureStorage），请求带鉴权头；列表分页查询；异常统一处理（网络异常提示重试、业务异常显示后端返回的消息）；离线场景本地缓存（SQLite），联网后同步。这就是你简历里"WPF 客户端与 ASP.NET Core Web API 协作"的口径。

### Q2：HIS 与 LIS/PACS/EMR 对接有哪些方式？怎么选？

**答案要点：**

| 方式 | 优点 | 缺点 | 适用 |
|------|------|------|------|
| 数据库中间表/视图 | 简单、低耦合、实时性好 | 耦合库结构、跨厂商难 | 院内网同库/信任环境 |
| WebService/REST API | 跨厂商标准、可鉴权、可追踪 | 实时性一般、接口规范要谈 | 跨系统常规交互 |
| 消息队列（RabbitMQ） | 异步解耦、可靠投递 | 要维护中间件、双方都要改造 | 高吞吐/解耦场景 |
| HL7/ASTM 消息 | 医疗行业标准 | 版本碎片化、字段映射工作量大 | 检验结果、医嘱标准交换 |

选型依据：对方厂商支持什么、实时性要求、网络环境（院内网 vs 公网）、事务性和运维成本。我们做 HIS/LIS/PACS/EMR 对接时先评估厂商接口文档，再定方案。

### Q3：接口对接怎么保证数据一致性/不重复不丢失？

**答案要点：**

1. **幂等**：用业务流水号做幂等键（Redis SETNX 先判重 + 数据库唯一索引兜底），重复请求不重复处理；
2. **状态机**：每条同步记录有状态（待同步/同步中/成功/失败），失败可重试；
3. **重试 + 补偿**：失败进重试表，定时任务重发，人工补报入口；
4. **日志追踪**：记录每次请求/响应原文，方便现场定位"数据去哪了"；
5. **对账**：定期按业务键核对双方数据量，发现差异自动补。

### Q4：CNRDS 全国数据上报怎么设计的？（项目亮点）

**答案要点：**

1. **抽取**：从血透系统按治疗记录/患者维度拉取待上报数据；
2. **转换**：按 CNRDS 规范做编码映射（诊断、治疗方案、单位等字典映射）、结构化转换；
3. **校验**：必填校验、值域校验、逻辑校验（如透析次数与治疗记录一致性），校验失败打标记、可修改重报；
4. **上报**：批量分页调用国家平台接口，逐批更新状态（待上报/上报中/成功/失败/需补报）；
5. **状态追踪**：本地状态表 + 界面展示，支持断点续传、补报、失败重试；
6. **稳定性**：上报任务放后台线程，不阻塞 UI；失败重试带退避。

### Q5：IVD 场景：检验结果怎么回传 LIS？

**答案要点：** 仪器出结果 → 客户端采集/解析 → 校验（项目、单位、参考范围、危急值标记）→ 本地确认 → 通过 HL7/ASTM 或厂商 API 回传 LIS → 记录回传状态（成功/失败）→ 失败重试 + 人工补传 → 回传日志可查。危急值要单独标记并触发提醒（这是医疗场景加分点）。

### Q6：医疗数据安全和合规要注意什么？

**答案要点：** 权限（RBAC，不同角色只能看对应数据）、操作审计（谁在什么时间改了什么）、传输加密（HTTPS/报文加密，如 SM4）、日志脱敏（身份证号/手机号打码）、数据不能随便出内网。可以提一句"我在发票网关做过 SM4 加密报文，对敏感数据加密有实际经验"。

---

## 8. 场景/设计题（准备 2 个就够）

### 场景 1：设计一套检验仪器数据管理客户端（IVD 典型题）

**回答框架：** 分层架构 + 设备抽象 + 异步数据流 + 异常兜底：

```
UI 层（WPF + MVVM）：患者/样本列表、结果展示、质控图、设备状态栏
      ↓ 命令/绑定
服务层：样本服务、结果服务、设备服务、上报服务
      ↓
设备层：IDeviceDriver 接口 + 各仪器驱动（串口/TCP 实现）
数据层：SQLite（本地缓存）/ Web API（上传中心系统）
```

关键设计：设备数据生产-消费模型（后台解析、节流 UI 更新）、断线重连 + 缓存补传、结果校验与危急值标记、操作日志。讲完加一句"这套思路我在 IOT 采集系统和血透设备对接里都实际落地过"。

### 场景 2：实时数据大屏/列表，数据一直来，界面不能卡

**回答框架：** 虚拟化列表 + 节流合并 + 后台处理 + 只更新可见数据。高频设备数据 100ms 合并一次 UI 更新；列表开 VirtualizingStackPanel；图表用增量追加而不是全量重绘。

### 场景 3：医院内网断网了，客户端怎么保证业务不中断？

**回答框架：** 本地 SQLite 缓存最近数据 → 操作先落本地 → 联网后按时间戳/版本增量同步（带幂等键）→ 同步进度可视、失败重试。离线写、在线补，是医疗现场（尤其血透、检验科）的真实刚需。

### 场景 4：客户端要升级，怎么做才不影响医院使用？

**回答框架：** 启动时检查版本 → 下载新版本包（校验 MD5/签名）→ 备份当前版本 → 替换 → 失败回滚；支持内网镜像下载；强制更新窗口和"稍后提醒"两种策略；老数据格式做好兼容迁移。

---

## 9. STAR 故事库（10 个）

> 用法：每个故事记住"一句话版"保底，被追问时展开完整版。数字处为占位符，请补真实数据后再用。

### 故事 1：多厂商设备统一接入（驱动抽象层）

**Use for:** 问题解决、架构设计、设备兼容

**完整版：**
- S：IOT 医疗设备采集系统要对接多家厂商的血压计、体重秤，每家协议不同（指令、帧格式、校验都不一样），如果每加一个厂商就改采集核心逻辑，系统会越来越难维护。
- T：我负责设计设备接入方案，让新设备能"低改动"接入。
- A：我抽象了统一设备驱动接口（Open/Send/DataReceived/Status），把每家厂商的差异封装成独立驱动类，核心采集逻辑只依赖接口；配置（厂商型号、串口参数、协议类型）外置到 JSON，驱动工厂按配置创建实例。
- R：新增设备只需"加一个驱动类 + 一份配置"，核心代码零改动；现场更换设备型号不再需要重新发版。【补真实数字：接入设备型号数、减少的开发/联调时间】

**精简版：** 我把多厂商设备接入做成了驱动抽象层：统一接口 + 驱动工厂 + 配置化，新设备只加类不改核心逻辑，现场换设备不用重新发版。

**一句话版：** 设备接入我做的是"接口抽象 + 配置化"，新厂商设备只加一个驱动类。

### 故事 2：串口协议解析与断线重连

**Use for:** 问题解决、稳定性、设备对接

**完整版：**
- S：HIS 里检验设备、读卡器通过串口通信，现场出现过数据帧错位、设备断电后程序卡死、重连不上等问题。
- T：我负责通信模块的稳定性，要保证设备数据准确、断线能自愈。
- A：我按协议文档实现接收缓冲 + 帧头/长度拆帧 + 校验（含 CRC/和校验），无效帧丢弃并记日志；用后台线程处理 DataReceived，解析结果节流投递到 UI；断线检测 + 指数退避重连 + 状态机，重连期间数据缓存，恢复后补发。
- R：设备通信稳定性明显提升，现场不需要人工重启程序。【补真实数字：故障率/人工干预次数下降】

**精简版：** 串口通信我做了拆帧校验 + 后台解析 + 断线自动重连（指数退避 + 状态机），数据不丢、界面不卡、现场不用人工重启。

**一句话版：** 串口通信我重点解决了拆帧校验和断线自愈。

### 故事 3：TCP 长连接设备在线监测

**Use for:** 问题解决、稳定性

**完整版：**
- S：采集终端和中心系统、设备之间需要实时在线状态，设备离线了要立刻知道，否则临床数据采集会中断。
- T：我负责实现设备在线监测和离线告警。
- A：用 TCP 长连接 + 应用层心跳（定时 ping、超时判离线）；独立接收循环处理字节流，半包缓存拆帧；离线/异常自动告警（UI 状态 + 提示），并自动重连。
- R：设备状态实时可视，离线能在第一时间被发现和处理。【补真实数字：离线发现时间从 X 缩短到 Y】

**精简版：** TCP 长连接我加了心跳检测 + 断线自动重连 + 离线告警，设备状态实时可控。

**一句话版：** 设备在线监测我用的是 TCP 长连接加心跳和告警。

### 故事 4：条码枪无侵入集成

**Use for:** 创造性解决问题、用户体验

**完整版：**
- S：HIS 客户端里很多单据行要扫条码，如果每个输入框都去接扫码事件，改动面大、代码侵入强。
- T：我负责让条码枪"扫哪填哪"，且不侵入现有界面代码。
- A：分析条码枪是键盘楔入式（扫码等于快速键盘输入），我用全局键盘钩子捕获输入流，识别完整扫码后自动路由到当前焦点单据行，并处理了防抖、输入法冲突、窗口失焦挂起等细节。
- R：扫码输入自动定位到焦点行，全客户端无侵入生效，单据录入效率提升。【补真实数字：录入效率提升幅度】

**精简版：** 条码枪我用全局键盘钩子做了"扫哪填哪"的无侵入集成，不用改任何一个输入框的代码。

**一句话版：** 条码枪集成我用了键盘钩子实现无侵入扫码输入。

### 故事 5：血透工作站 WPF + MVVM 客户端

**Use for:** WPF 实战、MVVM、业务开发

**完整版：**
- S：血透中心需要一套工作站客户端：患者接诊、透析排班、治疗记录、设备数据展示、医嘱管理，业务复杂且医护人员要快速操作。
- T：我负责 WPF 客户端核心业务模块开发。
- A：基于 WPF + MVVM 分层开发：View 只做 XAML 和数据绑定，ViewModel 用 CommunityToolkit.Mvvm 管理状态和命令（ObservableProperty + RelayCommand/AsyncRelayCommand），业务逻辑下沉到服务层；模块间用 WeakReferenceMessenger 通信（如治疗结束刷新患者列表）；结合医院流程优化界面操作路径（减少点击次数、常用操作置顶）。
- R：模块按期交付，医护人员实际使用顺畅。【补真实数字：使用科室数、上线院区数、操作效率提升】

**精简版：** 血透工作站是纯 WPF + MVVM 开发，我用 CommunityToolkit 管状态和命令、Messenger 解耦模块通信，按医护流程优化了操作路径。

**一句话版：** 我负责的血透工作站是标准 WPF + MVVM 架构，生产环境在用。

### 故事 6：HIS 与 LIS/PACS/EMR 多系统对接

**Use for:** 协作、集成、问题解决

**完整版：**
- S：HIS 要和 LIS、PACS、EMR 多个外部系统交换数据（检验结果、影像报告、病历），每个系统厂商不同、接口规范不同，联调周期长。
- T：我负责接口对接、数据转换和联调。
- A：先和各厂商确认接口方式与字段规范，设计统一的数据转换层（内部模型 + 映射），避免业务代码直接依赖外部报文格式；异常处理和重试机制兜底；和医院信息科、各厂商工程师排联调计划，现场逐条核对字段。
- R：多个系统对接按期完成联调验收，业务数据流转稳定。【补真实数字：对接系统数、联调周期】

**精简版：** HIS 对接 LIS/PACS/EMR 我做了统一数据转换层 + 异常重试，和厂商逐条核对字段，按期完成联调。

**一句话版：** 多医疗系统对接我靠"统一转换层 + 逐条联调核对"保证交付。

### 故事 7：客户端异步优化，界面不卡

**Use for:** 性能优化、线程调度

**完整版：**
- S：设备数据采集、数据同步、批量上报都是耗时任务，早期版本界面会卡顿，医生操作体验差。
- T：我负责优化客户端，保证后台任务执行时 UI 流畅。
- A：耗时任务全部改为 async/await + 后台线程（I/O 用真异步、CPU 密集用 Task.Run）；UI 更新走 Dispatcher 节流合并（高频设备数据 100ms 合并一次）；列表开虚拟化；大查询分页 + 索引优化。
- R：界面卡顿消除，后台采集/上报期间操作流畅。【补真实数字：卡顿场景、响应时间改善】

**精简版：** 我通过 async/await 化、UI 更新节流合并、列表虚拟化，把客户端的卡顿问题系统性地解决了。

**一句话版：** 客户端不卡，靠的是异步化 + 节流 + 虚拟化这三板斧。

### 故事 8：CNRDS 全国数据上报

**Use for:** 复杂业务、数据一致性

**完整版：**
- S：全国血液净化病例登记系统要求把血透治疗记录、患者信息、检验检查数据按国家规范上报，数据量大、校验规则多、上报失败要能补。
- T：我负责上报功能的开发：抽取、校验、补报、状态跟踪。
- A：做了本地状态表（待上报/成功/失败/需补报）+ 批量分页上报；上报前做必填/值域/逻辑校验，不合格数据可修改重报；失败自动重试 + 界面补报入口；上报任务后台执行不阻塞 UI。
- R：上报功能稳定运行，数据完整可追溯，补报不再靠人工核对。【补真实数字：上报机构数、数据量、成功率】

**精简版：** CNRDS 上报我做了"校验 + 状态机 + 批量补报"，失败可重试可追踪，不用人工核对。

**一句话版：** CNRDS 上报的核心是状态机和补报机制，数据一个不丢。

### 故事 9：多租户发票网关（工厂模式 + 分布式锁）

**Use for:** 设计能力、后端功底

**完整版：**
- S：多个地区要对接百望、金蝶等不同税务厂商开票，接口异构、地区规则不同，还要防止重复开票。
- T：我负责网关的扩展性和并发安全设计。
- A：用工厂模式把各家接口封装成统一实现，新增开票渠道只加实现类；Redis 分布式锁串行化多渠道开票请求；业务流水号 SETNX 幂等校验 + 数据库唯一索引兜底；MongoDB TTL 缓存第三方请求响应减少重复调用；SM4 加密报文防篡改。
- R：多渠道接入可扩展，重复开票被拦住。【补真实数字：接入渠道数、成本/调用量下降】

**精简版：** 发票网关我用工厂模式做多渠道扩展、Redis 锁 + 幂等防重复开票、SM4 加密传输。

**一句话版：** 发票网关的核心是工厂扩展 + 幂等防重 + 加密。

### 故事 10：主动做 WPF 医疗翻译工具

**Use for:** 主动性、学习能力、WPF 工具开发

**完整版：**
- S：团队处理国外医疗资料/术语翻译费时，没有统一工具。
- T：我主动提出并独立开发一个翻译辅助工具。
- A：用 WPF + MVVM 做客户端，OCR 识别图片文字，调大模型 API 翻译，SQLite 存术语库做校验和复用，做成任务流转管理。
- R：团队翻译效率提升，工具落地使用。【补真实数字：使用人数、效率提升】

**精简版：** 我主动做了一个 WPF 翻译工具（OCR + 大模型 + SQLite 术语库），团队翻译效率明显提升。

**一句话版：** 我主动用 WPF 做了个翻译工具，团队一直在用。

---

## 10. 行为面试 Q&A（8 题）

### 1. 讲一个你解决过的最复杂的技术问题

**考察点：** 问题拆解 + 技术深度 + 结果量化

**答案（STAR 要点）：** 最复杂的是多厂商设备协议兼容（故事 1/2）：串口/TCP 都有，协议各异，还要保证不卡不丢。我把它拆成三件事——驱动抽象（隔离差异）、拆帧校验（保证正确）、断线重连 + 缓存补发（保证可用）。每件单独验证，最后组合。结果：新设备接入成本大幅下降，现场稳定性提升。

**追问：** 如果有个设备协议文档不完整怎么办？→ 先抓包/串口监听看真实数据，反推帧结构，再和厂商工程师核对字段含义，先用模拟器联调再上真机。

### 2. 说一个计划之外出了问题的项目，你怎么处理的？

**考察点：** 应变 + 责任担当

**答案（STAR 要点）：** 现场联调时发现仪器厂商给的协议文档和真实数据不一致（长度字段含义对不上），导致解析错乱。处理：先临时在接收层加日志把原始帧打出来，对照真实数据定位差异，和厂商确认后修正解析；同时把原始报文日志保留，便于后续排查同类问题。结果：当天定位、修正后联调继续，没有影响上线计划（可如实说明是否有延期）。

### 3. 和医院信息科、设备厂商、其他系统团队联调时怎么协作？

**考察点：** 跨团队协作 + 沟通

**答案（STAR 要点）：** 对接 LIS/PACS/EMR 时，先明确各方接口文档和对接人，把字段清单列成核对表逐条确认；联调时间提前排期（医院现场时间宝贵）；遇到分歧（比如字段标准不一致）先拉齐双方需求再定映射方案，必要时请医院信息科仲裁。我负责的部分会先自测通过再约联调，不浪费对方时间。

### 4. 和同事意见不合怎么办？

**考察点：** 冲突处理

**答案（STAR 要点）：** 技术选型分歧，比如中间表还是 API 对接。我的做法：不争对错，先列两边的约束（厂商支持、实时性、维护成本）和代价，用一个小实验/原型验证关键不确定点，再一起定。原则是"用事实和数据对齐，而不是用职位和嗓门"。

### 5. 最有成就感的一件事？

**考察点：** 成就 + 价值判断

**答案（STAR 要点）：** 血透工作站从开发到多院区实际使用（故事 5），医护人员每天在用，设备数据稳定采集、界面不卡。成就感来自"我写的东西真的在临床环境里跑起来，帮医生护士节省时间"。另一个是 CNRDS 上报（故事 8），数据一个不丢、能补报，对机构合规很重要。

### 6. 哪次你超出了预期？

**考察点：** 主动性

**答案（STAR 要点）：** 主动做 WPF 翻译工具（故事 10）——本职之外发现团队痛点，自己用下班时间做了工具并推广使用。还有 IOT 采集系统的设备抽象层，本来没要求，我主动设计，后来新增设备全靠它省时间。

### 7. 讲一次失败或失误，你学到了什么？

**考察点：** 诚实 + 反思

**答案（STAR 要点）：** 有一次接口联调前，我没有先核对对方环境的数据规范，等到联调现场才发现字段映射有出入，导致联调比预期久。教训：**前置验证**——联调前先用样例数据把转换逻辑跑通、把字段清单核对完，再约现场；之后我所有对接都先做"数据预演"，再安排联调，再没出过这类问题。回答要点：真实、具体、有改进闭环，不装完美。

### 8. 收到过批评吗？怎么处理的？

**考察点：** 成长心态

**答案（STAR 要点）：** 代码评审时被指出异常处理不够完整（设备断开时资源没释放、日志不足）。我接受并补上 Dispose/释放逻辑和异常日志规范，之后把"资源释放 + 异常路径"列进自己的自测清单。批评的内容具体就改具体，模糊就问清楚再改。

---

## 11. 高频通用问题话术

### 为什么离开上一家公司？

**话术：** "在华墨做了四年医疗信息化，桌面端和医疗系统集成都完整走了一遍，能力上限基本是现有项目能给的。我现在想专注 WPF 桌面开发，去一家把桌面软件作为核心产品打磨的公司，把这块做深。贵公司的 IVD 桌面端正好是这条路。"

### 你最大的缺点是什么？

**话术（真实 + 认知 + 改进）：** "我有时候会过度关注代码的健壮性和细节，导致节奏偏慢。意识到之后，我会先和业务方对齐'验收标准'，按优先级推进，把完美主义用在关键路径上。另外我在学习用 Codex/Claude 这类 AI 工具做辅助，把重复工作提速，省下时间做更重要的事。"

### 为什么选桌面端/WPF 而不是后端？

**话术：** "后端我也能做，但桌面端的好处是能直接看到用户怎么用你的产品，交互反馈快。WPF 的表现力 + MVVM 的工程性，正好是我喜欢的那种'既有技术深度又能直接交付体验'的活。而且我有设备对接经验，桌面端 + 硬件是我最顺的组合。"

### 为什么想来医疗器械/IVD 行业？

**话术：** "我上一份工作就在医疗信息化（血透、HIS），对医疗业务和数据严肃性有认知：数据不能丢、流程要合规、现场要稳定。IVD 是把'仪器 + 软件 + 临床数据'连起来，正好是'桌面端 + 设备对接 + 医疗集成'的组合，等于把我的经验平移过来，上手最快。"

### 5 年规划？

**话术：** "1-2 年把 WPF 桌面端做深，成为团队里客户端和仪器对接都能扛的人；3-5 年往桌面端架构/技术负责人方向走，能独立负责一个产品线的客户端架构。"

### 期望薪资？

**话术（先回避）：** "薪资我更看重岗位匹配度，可以按贵公司的体系来。方便先了解一下这个岗位的薪资范围吗？"

**被逼问：** "结合我 5 年经验和桌面端 + 医疗设备对接的匹配度，期望在 12k-15k 之间，具体可以谈。"

---

## 12. 反问面试官（开放性问题）

### 问招聘经理/技术负责人

1. 这个岗位主要负责哪块客户端？是仪器配套软件，还是数据管理/报告类软件？
2. 目前客户端团队最大的技术挑战是什么（比如设备兼容、性能、现场问题多）？
3. 入职后 30/60/90 天，您希望新人先做出什么成果？

### 问团队成员/一线工程师

1. 客户端现在用的 WPF 版本和技术栈（CommunityToolkit、第三方控件）是怎样的？代码库是否已经 MVVM 化了？
2. 你们平时和仪器厂商、医院信息科联调多吗？典型的联调流程是什么样的？
3. 让我想象一下入职后第一周：我要先读哪块代码、和谁对接，才能最快上手？

---

## 13. IVD 公司背景笔记

> 该公司名称未知，面试前务必确认公司全称、产品线（生化/免疫/血球/分子/POCT 等）、软件形态。

- **桌面端在 IVD 公司的典型角色**：仪器控制/采集软件、检验数据管理系统、报告与质控软件、样本管理；很多 IVD 公司的软件就是 WPF/WinForms 写的（医疗器械软件合规要求高，更新谨慎）。
- **大概率问的方向**：仪器数据怎么采（串口/TCP/协议）、结果怎么回 LIS、断网/离线怎么办、界面卡不卡、现场联调怎么排、医疗数据安全与审计。
- **你可以主动提的行业词汇（展示熟悉度）**：ASTM/LIS2-A2（检验仪器双向通信标准）、HL7（系统间消息）、危急值、参考范围、样本条码追踪、质控（QC）、室内质控/Levey-Jennings 图、双工通信（双向）。
- **准备动作**：查公司主打仪器/试剂线；如果是做仪器配套软件，重点讲设备采集和协议解析；如果是做实验室信息系统/数据管理，重点讲 HIS/LIS 对接、报告、质控。
- **合规提示**：医疗器械软件可能有注册/合规要求（变更流程严），可以反问一句"软件版本变更走什么流程"，显得懂行。

---

## 14. 红牌清单与面试策略

### 避免踩的坑

1. **别贬低 WinForms**：不要"WinForms 太落后了"这种说法；换成"WinForms 适合快速工具类，WPF 适合复杂业务和现代化界面，我两边都做过，知道什么时候用哪个"——这反而显成熟。
2. **别撒谎/夸大**：简历没有的（如"我主导过整个血透系统架构"）不要认领；被问细节答不上来比诚实说"这块主要是团队 X 负责"更减分。
3. **别暴露医疗陌生感**：IVD 公司默认你懂检验业务流程；把"危急值、参考范围、样本、结果回传"这些词自然用出来。
4. **别只说做了什么，不说为什么**：每题回答都带一个"为什么这么设计/代价是什么"，这是你和只背八股的人的区别。
5. **别把设备对接讲成"调库"**：NModbus 只是工具，面试官要听你懂协议帧结构、校验、粘包、超时重发。
6. **自我介绍别背简历**：简历上写过的（职责列表）一句话带过，重点讲"你解决过什么真问题"。

### 面试前 48 小时检查清单

- [ ] 自我介绍口播稿录一遍，掐表 2 分钟，改到自然
- [ ] 三星题（第 3、4、5 节）过一遍，每道能自己讲出来而不是看稿
- [ ] 准备 2 个 STAR 故事（设备抽象层 + 血透工作站），能 60 秒讲完
- [ ] 补真实数字到 STAR 故事占位符（设备台数、科室数、效率提升、故障下降）
- [ ] 想好反问的 2-3 个问题（第 12 节）
- [ ] 查公司产品线，准备一句行业相关的话（第 13 节）
- [ ] 面试后当天把被问到的题和答得不好的点记下来，更新错题本（后续可让我按 resume-interview-qa 做复盘）

